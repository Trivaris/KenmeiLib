-- Table: manga_alternative_title
CREATE TABLE manga_alternative_title (
    manga_series_id     INTEGER REFERENCES manga_series(id),
    title               TEXT,

    UNIQUE (manga_series_id, title)
);
