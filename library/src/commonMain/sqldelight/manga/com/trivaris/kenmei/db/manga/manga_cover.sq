-- Table: cover
CREATE TABLE manga_cover (
    manga_series_id INTEGER PRIMARY KEY REFERENCES manga_series(id),
    large_webp_url  TEXT,
    small_webp_url  TEXT,
    large_jpeg_url  TEXT,
    small_jpeg_url  TEXT,
    twitter_url     TEXT,
    open_graph_url  TEXT
);
